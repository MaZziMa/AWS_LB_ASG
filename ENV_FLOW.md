# Environment Variables Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    LOCAL DEVELOPMENT                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  📁 backend/.env (MANUAL)                                        │
│  ┌────────────────────────────────────────┐                     │
│  │ DEBUG=True                              │                     │
│  │ DYNAMODB_ENDPOINT_URL=http://localhost  │ ──────┐            │
│  │ AWS_ACCESS_KEY_ID=                      │       │            │
│  │ AWS_SECRET_ACCESS_KEY=                  │       │            │
│  └────────────────────────────────────────┘       │            │
│                                                     │            │
│  🐍 Python App                                     │            │
│  ┌────────────────────────────────────────┐       │            │
│  │ from app.config import settings         │ ◄─────┘            │
│  │ # Reads .env using pydantic-settings    │                    │
│  └────────────────────────────────────────┘                     │
│                                                                  │
│  💾 Local DynamoDB (Docker)                                      │
│     docker run -p 8000:8000 amazon/dynamodb-local              │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                    DEPLOYMENT PROCESS                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  💻 Your Computer                                                │
│  ┌────────────────────────────────────────┐                     │
│  │ .\deploy-project.ps1                    │                     │
│  └──────────────┬─────────────────────────┘                     │
│                 │                                                │
│                 │ 1. Build frontend (npm run build)             │
│                 │ 2. Package backend + frontend → ZIP           │
│                 │ 3. Upload to S3                               │
│                 │ 4. Create DynamoDB tables                     │
│                 │ 5. Update IAM permissions                     │
│                 │ 6. Create launch template with user-data      │
│                 │ 7. Trigger instance refresh                   │
│                 ▼                                                │
│                                                                  │
│  ☁️  AWS S3 Bucket                                               │
│  ┌────────────────────────────────────────┐                     │
│  │ s3://course-reg-deploy-xxxxx/           │                     │
│  │   └── deploy-package.zip                │                     │
│  │       ├── backend/                      │                     │
│  │       └── frontend/                     │                     │
│  └────────────────────────────────────────┘                     │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                    EC2 INSTANCE (PRODUCTION)                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  📜 User-Data Script (Launch Template)                           │
│  ┌────────────────────────────────────────┐                     │
│  │ #!/bin/bash                             │                     │
│  │ aws s3 cp s3://bucket/package.zip .     │                     │
│  │ unzip package.zip                       │                     │
│  │ pip install -r requirements.txt         │                     │
│  │                                         │                     │
│  │ # AUTO-CREATE .env FILE                 │                     │
│  │ cat > .env << ENV                       │                     │
│  │ APP_NAME=Course Registration            │                     │
│  │ DEBUG=False                             │                     │
│  │ DYNAMODB_REGION=us-east-1               │                     │
│  │ SECRET_KEY=$(openssl rand -hex 32)      │ ◄─── Generated!    │
│  │ AWS_ACCESS_KEY_ID=                      │ ◄─── Empty (IAM)   │
│  │ AWS_SECRET_ACCESS_KEY=                  │ ◄─── Empty (IAM)   │
│  │ ...                                     │                     │
│  │ ENV                                     │                     │
│  │                                         │                     │
│  │ systemctl start course-registration     │                     │
│  └────────────────────────────────────────┘                     │
│                 │                                                │
│                 ▼                                                │
│                                                                  │
│  📁 /opt/course-registration/backend/.env (AUTO-GENERATED)       │
│  ┌────────────────────────────────────────┐                     │
│  │ APP_NAME=Course Registration System     │                     │
│  │ DEBUG=False                             │                     │
│  │ ENVIRONMENT=production                  │                     │
│  │ DYNAMODB_REGION=us-east-1               │                     │
│  │ SECRET_KEY=<random-32-bytes>            │ ✅ Unique per      │
│  │ AWS_ACCESS_KEY_ID=                      │    instance        │
│  │ AWS_SECRET_ACCESS_KEY=                  │                     │
│  └────────────────────────────────────────┘                     │
│                 │                                                │
│                 ▼                                                │
│                                                                  │
│  🐍 FastAPI Application                                          │
│  ┌────────────────────────────────────────┐                     │
│  │ uvicorn main:app --port 8000            │                     │
│  │                                         │                     │
│  │ from app.config import settings         │ ◄─── Reads .env    │
│  │                                         │                     │
│  │ ┌─────────────────────────────────┐    │                     │
│  │ │ settings.AWS_ACCESS_KEY_ID      │    │                     │
│  │ │ # Empty → boto3 uses IAM role! │    │                     │
│  │ └─────────────────────────────────┘    │                     │
│  └────────────────────────────────────────┘                     │
│                 │                                                │
│                 ▼                                                │
│                                                                  │
│  🔐 IAM Instance Profile: MyEC2Profile                           │
│  ┌────────────────────────────────────────┐                     │
│  │ Attached Policies:                      │                     │
│  │  ✓ DynamoDB: Full access CourseReg_*    │                     │
│  │  ✓ S3: Read access deployment bucket    │                     │
│  │  ✓ CloudWatch: Logs and metrics         │                     │
│  │  ✓ SSM: Session Manager access          │                     │
│  └────────────────────────────────────────┘                     │
│                 │                                                │
│                 ▼                                                │
│                                                                  │
│  ☁️  AWS Services (No credentials needed!)                       │
│  ┌────────────────────────────────────────┐                     │
│  │ DynamoDB: CourseReg_Users, etc.         │                     │
│  │ S3: course-reg-deployment-xxxxx         │                     │
│  │ CloudWatch: Logs + Metrics              │                     │
│  │ SQS: Course_project.fifo (optional)     │                     │
│  └────────────────────────────────────────┘                     │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                    UPDATING VARIABLES                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Option 1: Redeploy (Fresh Start)                               │
│  ┌────────────────────────────────────────┐                     │
│  │ .\deploy-project.ps1                    │                     │
│  │ # Creates new instance with new .env    │                     │
│  └────────────────────────────────────────┘                     │
│                                                                  │
│  Option 2: Update Running Instance                              │
│  ┌────────────────────────────────────────┐                     │
│  │ .\update-env.ps1 -InstanceIp X.X.X.X \  │                     │
│  │   -EnvVars @{                           │                     │
│  │     DEBUG = "True"                      │                     │
│  │     SQS_QUEUE_URL = "..."               │                     │
│  │   }                                     │                     │
│  │                                         │                     │
│  │ # SSHs, updates .env, restarts service  │                     │
│  └────────────────────────────────────────┘                     │
│                                                                  │
│  Option 3: Manual SSH Edit                                      │
│  ┌────────────────────────────────────────┐                     │
│  │ ssh -i key.pem ec2-user@X.X.X.X         │                     │
│  │ sudo nano /opt/.../backend/.env         │                     │
│  │ sudo systemctl restart course-reg...    │                     │
│  └────────────────────────────────────────┘                     │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                    KEY POINTS                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ✅ EC2 uses IAM Role for AWS credentials (no hardcoding)        │
│  ✅ .env file auto-generated on each deployment                  │
│  ✅ Secret keys unique per instance (security)                   │
│  ✅ Can update variables without full redeploy                   │
│  ✅ Same config.py code works locally and production             │
│                                                                  │
│  ❌ Never commit AWS credentials to .env                         │
│  ❌ Never hardcode secrets in code                               │
│  ❌ Don't copy local .env to production                          │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## Quick Reference Commands

```powershell
# View current configuration
.\view-env.ps1

# Update variables
.\update-env.ps1 -InstanceIp 34.207.173.116 -EnvVars @{
    SQS_QUEUE_URL = "https://sqs.us-east-1.amazonaws.com/171308902397/Course_project.fifo"
}

# Full redeploy
.\init-database.ps1
.\deploy-project.ps1
```
